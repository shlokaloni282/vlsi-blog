# Full Custom vs Semi-Custom IC Design

GitHub Pages-ready static website. Open `index.html` locally to preview, or upload the contents of this folder to the root of a GitHub repository and enable GitHub Pages.

Files: `index.html`, `style.css`, `script.js`, and `images/`.
